# Open Meteo non richiede API key per questo MVP.

# Brevo (opzionale, solo per invio email)
BREVO_API_KEY=
EMAIL_FROM=
EMAIL_TO=
EMAIL_FROM_NAME=Taxi Opportunity Radar

# Solo per ambienti con problemi di certificati TLS lato Node.
# Lascia vuoto o false in produzione.
EVENTS_ALLOW_INSECURE_TLS=false
